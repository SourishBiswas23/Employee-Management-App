import 'package:flutter/material.dart';

class EditEmployeeListScreen extends StatelessWidget {
  const EditEmployeeListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Edit employee list screen'),
      ),
    );
  }
}
